{
	"name": "strava-mcp",
	"version": "0.0.0",
	"private": true,
	"description": "Fork of gabeperez/strava-mcp (SportMCP) ported from Cloudflare Workers + KV to Vercel + Upstash Redis.",
	"scripts": {
		"dev": "vercel dev",
		"deploy": "vercel deploy --prod --yes",
		"typecheck": "tsc --noEmit",
		"test": "vitest"
	},
	"devDependencies": {
		"typescript": "^5.5.4",
		"vercel": "^39.0.0",
		"vitest": "~3.2.0"
	},
	"dependencies": {
		"@upstash/redis": "^1.34.0",
		"hono": "^4.9.8"
	}
}
